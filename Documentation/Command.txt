1.Migrate :
    1.1.dotnet ef migrations add InitialCreate
    1.2.dotnet ef database update

    

2.Build and run :
    2.1. dotnet clean OdaMeClone.csproj
    2.2. dotnet restore OdaMeClone.csproj
    2.3. dotnet build OdaMeClone.csproj 
    2.4. dotnet run OdaMeClone.csproj 

3.prettier:
    dotnet format OdaMeClone.csproj
    dotnet format whitespace OdaMeClone.csproj
    dotnet format style OdaMeClone.csproj
    dotnet format analyzers OdaMeClone.csproj



4. Check DB :
    psql -U postgres -p 5432 -d postgres